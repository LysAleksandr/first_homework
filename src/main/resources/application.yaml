server:
  port: 8080

app:
  name: "ToDoListManager"
  version: "1.0"

---
spring:
  config:
    activate:
      on-profile: dev

server:
  port: 8081

logging:
  level:
    com.mipt.lysaleksandr.todolistmanager: DEBUG

---
spring:
  config:
    activate:
      on-profile: prod

server:
  port: 80

logging:
  level:
    com.mipt.lysaleksandr.todolistmanager: WARN